all:
  children:
    Orchestrators:
      hosts:
        fedora:
          ansible_host: fedora-srv
          ansible_user: kinga

    Endpoints:
      hosts:
        ansible-target:
          ansible_host: ansible-target
          ansible_user: ansible
