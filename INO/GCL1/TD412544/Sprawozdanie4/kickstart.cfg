# Generated by Anaconda 40.22.3
# Generated by pykickstart v3.52
#version=DEVEL
# Use text mode install
text

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang en_US.UTF-8

url --mirrorlist="http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-40&arch=x86_64"
repo --name=updates-released --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f40&arch=x86_64

network --hostname=jorjor-wel

%packages
@^server-product-environment
docker

%end

# Run the Setup Agent on first boot
firstboot --enable
# Do not configure the X Window System
skipx

# Generated using Blivet version 3.9.1
ignoredisk --only-use=sda
# System bootloader configuration
bootloader --location=mbr --boot-drive=sda
autopart
# Partition clearing information
clearpart --all

# System timezone
timezone Europe/Warsaw --utc

#Root password
rootpw --lock
user --groups=wheel --name=ullei --password=$y$j9T$HAGfT4fgQcI0aPG9t4hgfSpj$jFCBUCnd95B3vMKJHnsFhGgX96AKwN1o2vEKUkLhmA. --iscrypted

%post --erroronfail --log=/root/post.log
usermod -aG docker root
systemctl enable docker

cat << 'EOF' > /etc/systemd/system/docker-irssi-deploy.service
[Unit]
Description=Download and run Irssi container
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker pull ullei/irssi:1.0.0
ExecStart=/usr/bin/docker run -d -it --name irssi ullei/irssi:1.0.0

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable docker-java-deploy.service
systemctl start docker-java-deploy.service

%end
