# Generated by Anaconda 40.22.3
# Generated by pykickstart v3.52
#version=DEVEL
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

#Network information
network  --bootproto=dhcp --device=enp0s3 --ipv6=auto --activate
network  --hostname=kopczys

# Use network installation
url --mirrorlist="http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-40&arch=x86_64"
repo --name=updates-released --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f40&arch=x86_64

%packages
@^minimal-environment
@container-management
@^server-product-environment
moby-engine
%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.9.1
ignoredisk --only-use=sda
autopart
# Partition clearing information
clearpart --all

# System timezone
timezone Europe/Warsaw --utc

#Root password
rootpw --lock
user --groups=wheel --name=kopczys --password=$y$j9T$NYyNQ.i2NacvrtChkjsqJRIc$u1Kl02qVjJwoJWANNReMDKmGLZCRF0BBvqLnmMuG8H5 --iscrypted --gecos="Hubert Kopczyński"

#Sekcja post
%post --erroronfail
usermod -aG docker root

systemctl enable docker

cat << 'EOF' > /etc/systemd/system/docker-java-deploy.service

[Unit]
Description=Pobranie i uruchomienie konteneru Java
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker pull kopczys/node-app
ExecStart=/usr/bin/docker run -d -p 3000:3000 --name node-app-container kopczys/node-app

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable docker-java-deploy.service
systemctl start docker-java-deploy.service

%end

reboot
