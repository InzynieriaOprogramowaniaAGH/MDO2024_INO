apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
  labels:
    app: nginx-deploy1
spec:
  replicas: 4
  selector:
    matchLabels:
      app: nginx-deploy1
  template:
    metadata:
      labels:
        app: nginx-deploy1
    spec:
      containers:
      - name: mynginx
        image: nbsss/my-nginx:v1
        ports:
        - containerPort: 80
