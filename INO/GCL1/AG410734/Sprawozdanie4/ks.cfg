# Generated by Anaconda 38.23.4
# Generated by pykickstart v3.47
#version=DEVEL
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

# Network information
network  --bootproto=dhcp --device=eth0 --ipv6=auto --activate
network  --hostname=ignite-myhostname.localdomain

# Network installation
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-38&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f38&arch=x86_64

%packages
@^server-product-environment
moby-engine
wget
curl
%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.7.1
ignoredisk --only-use=sda
autopart
# Partition clearing information
clearpart --all

# System timezone
timezone Europe/Warsaw --utc

# Root password
rootpw --lock
user --groups=wheel --name=ignite --password=$y$j9T$udPBpd.XCwrczYvte4YM37Mj$/jI8vRhQgoc84/6uurFbJpt4.2wjji6DQdkMrebzwE/ --iscrypted

# Execute post installation commands (throw error on fail and save logs)
%post --erroronfail --log=/root/ks-post.log

# Add root user to the docker group
usermod -aG docker root
systemctl enable docker

# Create systemd service for irssi container
cat << 'EOF' > /etc/systemd/system/irssi-docker.service
[Unit]
Description=Download docker and run irssi container
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/bin/docker pull agnieszka123/deploy:1.0-1
ExecStart=/usr/bin/docker run -d --name irssi_container agnieszka123/deploy:1.0-1

[Install]
WantedBy=multi-user.target
EOF

# Reload systemd to apply new service and enable it
systemctl daemon-reload
systemctl enable irssi-docker.service
systemctl start irssi-docker.service

%end
