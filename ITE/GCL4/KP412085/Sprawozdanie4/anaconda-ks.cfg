# Generated by Anaconda 39.32.6
# Generated by pykickstart v3.48
#version=DEVEL
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

# Network information
network  --bootproto=dhcp --device=eth0 --ipv6=auto --activate
network  --hostname=client.localdomain

# Network installation
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-39&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f39&arch=x86_64


%packages
@^custom-environment

%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.8.1
ignoredisk --only-use=sda
autopart
# Partition clearing information
clearpart --all

# System timezone
timezone Europe/Warsaw --utc

# Root password
rootpw --lock
user --groups=wheel --name=client --password=$y$j9T$udPBpd.XCwrczYvte4YM37Mj$/jI8vRhQgoc84/6uurFbJpt4.2wjji6DQdkMrebzwE/ --iscrypted

# Execute post installation commands (throw error on fail and save logs)
%post --erroronfail --log=/root/ks-post.log

# Install and run docker
dnf install -y docker
systemctl start docker

# Configure docker as a root
groupadd docker
usermod -aG docker $USER

# Unable docker to run after reboot
systemctl enable docker.service
systemctl enable containerd.service

# Pull image
docker pull kacperpap/irssi-deploy:1.0-1

# Run container in interactive way
docker run -it --name irssi kacperpap/irssi-deploy:1.0-1

%end
